%h1 Listing games
%table
  %tr
    %th.title Title
    %th.rating Rating
    %th.date Date Added
    %th.buttons
  - @games.each do |game|
    %tr
      %td.title= game.title
      %td.rating= game.rating
      %td.date= game.date