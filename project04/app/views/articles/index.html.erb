<div class='header'>
    <h1>Articl.es</h1>
    <h4>... with a name .com hipsters will love</h4>
</div>

<h2>All the World's Articles</h2> 
<span class='new_article_button'><%= link_to image_tag('plus.png')+' Create an Article', new_article_path, :title => 'Creates a new article' %></span>

<br /><br /><br />

<table>
        <tr>
            <th class ='title'>Title</th>
            <th class ='author'>Author Name</th>
            <th class ='article_body'>Body</th>
            <th class ='date'>Creation Date</th>
            <th class ='buttons'></th>
        </tr>
    
</table>
<div class='articles'>
    <table>
        <% @articles.each do |article| %>
        <tr class='<%= cycle('odd_row', 'even_row') %>'>
            <td class='title'><%= truncate(article.title, :length => 20) %></td>
            <td class='author'><%= truncate(article.author, :length => 20) %></td>
            <td class='article_body'><%= truncate(article.body, :length => 40) %></td>
            <td class='date'><%= article.date_posted.strftime("%b. %d, %Y") %></td>
            <td class='buttons'>
                <%= link_to image_tag("search.png"), article, :title => 'View article' %>
                <%= link_to image_tag('edit.png'), edit_article_path(article), :title => 'Edit article' %>
                <%= link_to image_tag('destroy.png'), article, :title => 'Delete article', :confirm => 'Are you sure?', :method => :delete%>
            </td>
        </tr>
        <% end %>
            
    </table>
</div>  

<br />
<%= @articles.count %> articles.


